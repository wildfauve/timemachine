<% if model_obj.errors.any? %>  
    <div id="errorExplanation">  
      <h5 class="subheader"><%= pluralize(model_obj.errors.count, "error") %> prohibited this record from being saved:</h5>  
	   <% model_obj.errors.messages.each do |field, msg| %>  
	   	<h6 class="subheader"><%= "#{field}: #{msg[0]}" %></h6>  
	   <% end %>  
    </div>  
<% end %>
